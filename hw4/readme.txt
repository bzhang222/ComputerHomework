
1 start new project

2 copy city.bin and popIndex to the folder

3 load popIndex

4 print out each city in population ascending order:

id,name,country-code,district,population

5 create a mycities.csv file by fopen a file in "w" mode,
 replace the above printf with fprintf(fp , fmt , ...) to dump all info in the opened file
close the file


at the end of hw, the binary city.bin is converted back to csv format, 

6 open mycities.csv with excel file
